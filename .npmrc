# npm 配置 - 跨平台兼容
registry=https://registry.npmmirror.com

# 启用依赖提升（类似npm的hoisting）
pnpm config set shamefully-hoist true --global

# 缓存路径
cache=%USERPROFILE%/.npm/cache
prefix=%USERPROFILE%/.npm-global
