# npm 配置 - 跨平台兼容
registry=https://registry.npmmirror.com

# 全局安装路径
prefix=%USERPROFILE%/.npm-global

# 缓存路径
cache=%USERPROFILE%/.npm/cache

# 强制使用全局包
global=true

# 不创建本地 node_modules
link=true
