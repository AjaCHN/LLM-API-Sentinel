# 国际化 (i18n)

本项目使用 `next-intl` 实现多语言支持。

## 语言文件
位于 `/app/locales/` 目录下，包含：
- `en.json` (基准)
- `zh-cn.json` (简体中文)
- `zh-tw.json` (繁体中文)
- ... (其他语言)

## 添加新语言
1. 在 `/app/locales/` 下创建 `{lang}.json`。
2. 复制 `en.json` 的结构。
3. 翻译所有 Key。
